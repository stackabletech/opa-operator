# Resource info fetcher

Fetches resource metadata from a data catalog, and exposes it in a normalized format for OPA rules to read.

It is deployed by the Stackable OPA Operator, and is not recommended for standalone use.

## Supported backends

- `datahub` - Datahub
