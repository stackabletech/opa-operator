---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-pod
  labels:
    app: test-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-pod
  template:
    metadata:
      labels:
        app: test-pod
    spec:
      containers:
      - name: test-pod
        image: oci.stackable.tech/sdp/testing-tools:0.3.0-stackable0.0.0-dev
        stdin: true
        tty: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "500m"
