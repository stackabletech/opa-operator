---
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: >-
      helm install postgresql-superset
      --namespace $NAMESPACE
      --version 12.5.6
      -f 22-postgresql-superset-values.yaml
      --repo https://charts.bitnami.com/bitnami postgresql
      --wait
    timeout: 600
