---
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 600
commands:
  # Sample data finished loading into GMS.
  - script: kubectl -n $NAMESPACE wait --for=condition=complete --timeout=590s job/sample-data-ingest
---
# The credentials Secret for the resource-info-fetcher exists.
apiVersion: v1
kind: Secret
metadata:
  name: datahub-rif-credentials
